# GPU/CUDA environment -- separate from the Mac's MLX environment.
# pip install -r requirements.txt  (inside a fresh conda/venv on the GPU machine)

torch
transformers
accelerate
peft
bitsandbytes
trl
datasets
sentence-transformers
beautifulsoup4
lxml
requests
